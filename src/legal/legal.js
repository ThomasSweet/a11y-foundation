// Entry for the standalone legal pages. No app, no runtime behaviour — it only
// pulls in the shared design system (tokens, reset, base, utilities) and the
// page layout, so the legal pages match the site with zero JavaScript of their
// own. (Keeping these as plain static documents fits the "little to no JS" ethos.)
import '../styles/index.css'
import './legal.scss'
